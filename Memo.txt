

現在のユーザーだけ、常にスクリプトを許可できるようにする
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

「インターネットゾーン」のマーク（Zone.Identifier）が付いている可能性があります。
ブロックされる場合は、次のコマンドでブロックを解除
Unblock-File .\setup-ntfy.ps1
Unblock-File .\notify.ps1


ファイルを「UTF-8として読み込み、BOM付きUTF-8として書き直す」処理です。ダウンロード後にBOMが失われていても、これでその場で修正できます。
$content = Get-Content .\setup-ntfy.ps1 -Raw -Encoding UTF8
$utf8Bom = New-Object System.Text.UTF8Encoding $true
[System.IO.File]::WriteAllText((Resolve-Path .\setup-ntfy.ps1).Path, $content, $utf8Bom)



powershell -ExecutionPolicy Bypass -File C:\Users\kenic\Dropbox\gitdir\setup-ntfy\setup-ntfy.ps1



＜setup-ntfy.ps1をPC起動時に自動で実行するようにしたい＞
PowerShellコマンドで一括登録

管理者権限のPowerShellで、次を1回実行すれば同じ内容のタスクが登録されます
$scriptPath = "C:\Users\kenic\Dropbox\gitdir\setup-ntfy\setup-ntfy.ps1"
$action = New-ScheduledTaskAction -Execute "powershell.exe" `
    -Argument "-ExecutionPolicy Bypass -File `"$scriptPath`""
$trigger = New-ScheduledTaskTrigger -AtLogOn
$principal = New-ScheduledTaskPrincipal -UserId $env:USERNAME -LogonType Interactive -RunLevel Highest
Register-ScheduledTask -TaskName "ntfy-autostart" -Action $action -Trigger $trigger -Principal $principal

タスクが登録されているか確認する方法
Get-ScheduledTask -TaskName "ntfy-autostart"

ログオンし直さなくても、タスクスケジューラのGUIか、次のコマンドでその場実行できます。
Start-ScheduledTask -TaskName "ntfy-autostart"


タスクを止めたい・削除したい場合
Unregister-ScheduledTask -TaskName "ntfy-autostart" -Confirm:$false


＜登録済みタスクの設定変更（管理者権限のPowerShellで実行）＞
・コンソールを表示する（-WindowStyle Hidden を外す）
・エラーで止まってもウィンドウを残す（-NoExit）
・バッテリー駆動でも開始・継続する
・実行時間の上限（既定72時間）をなくす
※ウィンドウを閉じるとntfyサーバーも止まるので、邪魔なら最小化する

実行中のタスクを止める（入力待ちで止まっている場合など）
Stop-ScheduledTask -TaskName "ntfy-autostart"

設定を変更する
$scriptPath = "C:\Users\kenic\Dropbox\gitdir\setup-ntfy\setup-ntfy.ps1"
$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoExit -ExecutionPolicy Bypass -File `"$scriptPath`""
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -ExecutionTimeLimit ([TimeSpan]::Zero)
Set-ScheduledTask -TaskName "ntfy-autostart" -Action $action -Settings $settings

変更内容を確認する
(Get-ScheduledTask -TaskName "ntfy-autostart").Actions.Arguments
(Get-ScheduledTask -TaskName "ntfy-autostart").Settings | Format-List DisallowStartIfOnBatteries, StopIfGoingOnBatteries, ExecutionTimeLimit


テスト通知の送り方
C:\Users\kenic\Dropbox\gitdir\setup-ntfy\notify.ps1 -Message "テストtest"


＜状態が Up になっていることを確認します＞
Get-NetAdapter | Where-Object InterfaceDescription -match Bluetooth



<Bluetooth側のPCのIP>って接続するたびに毎回変わってしまうの？
mDNS / ホスト名でアクセス: http://<PCのコンピュータ名>.local:8080 のような形にできれば、IPが変わっても追従できますが、AndroidのBluetooth PAN環境でmDNSが動くかは機種依存で確実ではありません
＜mDNS用のポート（UDP 5353）をファイアウォールで許可する＞
管理者権限のPowerShellで実行します。
New-NetFirewallRule -DisplayName "mDNS 5353" -Direction Inbound -Protocol UDP -LocalPort 5353 -Action Allow -Profile Any
->ダメだ


＜PC側の設定について＞
https://keep.google.com/#NOTE/1p1jHD9BfAV3LaCgUHSevKMJmqHmtH-4tWE8NRxbiMMdW88RkNdkbjeivNu9T_w


